
// If a variable or function doesn't have static or external,
// C++ allows other files to use extern on it
int externalLinkage = 12;
int externalLocal = 5;

int minus(int x, int y) {
	return x - y;
}
